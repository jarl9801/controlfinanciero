import { Typography } from '@mui/material';

export default function Transactions() {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Transacciones
      </Typography>
      <Typography variant="body1">
        Módulo de registro de operaciones con partida doble
      </Typography>
    </div>
  );
}
