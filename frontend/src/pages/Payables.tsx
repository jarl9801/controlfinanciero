import { Typography } from '@mui/material';

export default function Payables() {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Cuentas por Pagar
      </Typography>
      <Typography variant="body1">
        Módulo de gestión de proveedores y pagos
      </Typography>
    </div>
  );
}
