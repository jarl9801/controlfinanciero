import { Typography } from '@mui/material';

export default function CashFlow() {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Flujo de Caja
      </Typography>
      <Typography variant="body1">
        Módulo de proyecciones y control de tesorería
      </Typography>
    </div>
  );
}
