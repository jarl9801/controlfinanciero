import { Typography } from '@mui/material';

export default function Receivables() {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Cuentas por Cobrar
      </Typography>
      <Typography variant="body1">
        Módulo de gestión de clientes y cobros
      </Typography>
    </div>
  );
}
