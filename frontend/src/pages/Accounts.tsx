import { Typography } from '@mui/material';

export default function Accounts() {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Catálogo de Cuentas
      </Typography>
      <Typography variant="body1">
        Módulo de gestión del plan contable
      </Typography>
    </div>
  );
}
